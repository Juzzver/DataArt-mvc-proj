@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        Hello world (из представления)
        @ViewBag.Greeting (из представления)
        @{var x = "eter"; Response.Write(x);}

        <p>Мы собираемся на захватывающую вечеринку.</p>

        <!-- Html.ActionLink() - это вспомогательный метод HTML для визуализации ссылок-->
        @Html.ActionLink("Registration", "RegistrationForm") <br />
        @Html.ActionLink("Create Test", "CreateTestForm")

    </div>
</body>
</html>
